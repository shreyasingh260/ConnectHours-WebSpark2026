import {redirect} from 'next/navigation';import {getSession} from '@/lib/auth';import {getOfficeHoursForFaculty,listAppointmentsForUser} from '@/lib/data';import Shell from '@/components/layout/Shell';import {Card} from '@/components/ui/card';export default function Calendar(){const s=getSession();if(!s||s.role!=='faculty')redirect('/login');const h=getOfficeHoursForFaculty(s.id),a=listAppointmentsForUser(s.id,'faculty');const days=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];return <Shell role='faculty' name={s.name} title='Calendar'><div><p className='text-sm text-marigold-600'>Schedule view</p><h1 className='font-display text-3xl font-semibold'>Office-hours calendar</h1><p className='mt-1 text-sm text-ink-400'>A simple weekly view of your published availability.</p></div><div className='mt-6 grid gap-3 md:grid-cols-2 lg:grid-cols-3'>{days.slice(1,6).map((d,idx)=>{const day=idx+1;return <Card key={d} className='p-5'><h2 className='font-display font-semibold'>{d}</h2><div className='mt-3 space-y-2'>{h.filter(x=>x.day_of_week===day).map(x=><div key={x.id} className='rounded-xl bg-teal-50 p-3 text-sm'><p className='font-medium'>{x.start_time} – {x.end_time}</p><p className='text-xs text-teal-600'>{x.location}</p></div>)}{!h.some(x=>x.day_of_week===day)&&<p className='text-sm text-ink-300'>No office hours</p>}</div></Card>})}</div><Card className='mt-6 p-6'><h2 className='font-display text-lg font-semibold'>Upcoming bookings</h2><div className='mt-4 space-y-2'>{a.filter(x=>['pending','confirmed'].includes(x.status)).slice(0,8).map(x=><div key={x.id} className='flex justify-between rounded-xl bg-ink-50 p-3 text-sm'><span>{x.studentName}</span><span>{x.start_at}</span></div>)}</div></Card></Shell>}
