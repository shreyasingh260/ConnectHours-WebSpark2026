'use client';import {useEffect,useState} from 'react';import {Card} from '@/components/ui/card';export default function Analytics(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/faculty').then(x=>x.json()).then(d=>{const a=d.appointments||[];setRows(a)})},[]);const total=rows.length,completed=rows.filter(x=>x.status==='completed').length,no=rows.filter(x=>x.status==='no_show').length;return <main className='mx-auto max-w-5xl p-4 sm:p-6'><p className='text-sm text-marigold-600'>Faculty insights</p><h1 className='font-display text-3xl font-semibold'>Your utilization</h1><p className='mt-1 text-sm text-ink-400'>Lightweight operational analytics from the demo appointment history.</p><div className='mt-6 grid gap-4 md:grid-cols-3'><K label='Appointments' v={total}/><K label='Completion rate' v={`${total?Math.round(completed/total*100):0}%`}/><K label='No-show rate' v={`${total?Math.round(no/total*100):0}%`}/></div><Card className='mt-6 p-6'><h2 className='font-display text-lg font-semibold'>Attendance signals</h2><div className='mt-5 h-4 overflow-hidden rounded-full bg-ink-50'><div className='h-full bg-teal-500' style={{width:`${total?Math.round(completed/total*100):0}%`}}/></div><div className='mt-3 flex justify-between text-xs text-ink-400'><span>Completed {completed}</span><span>No-show {no}</span><span>Other {total-completed-no}</span></div></Card></main>};function K({label,v}:any){return <Card className='p-5'><p className='text-xs text-ink-400'>{label}</p><p className='mt-2 font-display text-3xl font-semibold'>{v}</p></Card>}
