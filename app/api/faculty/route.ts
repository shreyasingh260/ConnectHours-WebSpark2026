import {NextResponse} from 'next/server';import {getSession} from '@/lib/auth';import {listAppointmentsForUser,listFaculty,getDepartments,getAllSubjects} from '@/lib/data';
export async function GET(req:Request){const s=getSession();if(!s||s.role!=='faculty'&&s.role!=='admin'&&s.role!=='student')return NextResponse.json({error:'Unauthorized'},{status:401});const {searchParams}=new URL(req.url);return NextResponse.json({faculty:listFaculty({search:searchParams.get('search')||'',department:searchParams.get('department')||'',subject:searchParams.get('subject')||''}),departments:getDepartments(),subjects:getAllSubjects(),appointments:s.role==='faculty'?listAppointmentsForUser(s.id,'faculty'):[]})}
