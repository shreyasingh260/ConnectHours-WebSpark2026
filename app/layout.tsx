import type {Metadata} from 'next';import '@fontsource-variable/lexend';import '@fontsource-variable/inter';import './globals.css';import {ToastProvider} from '@/components/ui/toast';export const metadata:Metadata={title:'ConnectHours · ABESEC Faculty Appointment Portal',description:'Book faculty office hours, manage availability, and track department analytics.'};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang='en'><body className='font-body antialiased'><ToastProvider>{children}</ToastProvider></body></html>}
